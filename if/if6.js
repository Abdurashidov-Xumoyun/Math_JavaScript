// x​ haqiqiy son berilgan. Quyidagi funksiyani hisoblang.

// 1 - Javob: 12
// 2 - Javob: -3
// 3 - Javob: -24
// 4 - Javob: 0

{
  let x = 6;
  let fx;

  if (x < -2 || x > 2) {
    console.log(fx = 2 * x);
  } else {
    console.log(fx = -3 * x);
  }
}

{
  let x = 1;
  let fx;

  if (x < -2 || x > 2) {
    console.log(fx = 2 * x);
  } else {
    console.log(fx = -3 * x);
  }
}

{
  let x = -12;
  let fx;

  if (x < -2 || x > 2) {
    console.log(fx = 2 * x);
  } else {
    console.log(fx = -3 * x);
  }
}

{
  let x = 0;
  let fx;

  if (x < -2 || x > 2) {
    console.log(fx = 2 * x);
  } else {
    console.log(fx = -3 * x);
  }
}